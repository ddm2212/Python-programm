'''       Tuple

        1). Tuple is immutable (value Can not be Change at Any cost)
        2). Fasrer than list
        3). Used for sequencing the data  ( denoted by t=()
        4). Their should be more than one value in tuple sepeated by comma
'''

t=(10,20,'more')
print(t,type(t))
t=50
print(t,type(t))               # If you give Single value into then that value datatype is assigned to tuple (only applicat into single value to tuple)

